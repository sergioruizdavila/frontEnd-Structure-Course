/**
* config()
* @description - Rest API config file
*/

(function() {
    'use strict';

    angular
        .module('psApp.core.restApi', [])
        .config(config);


        function config() {
        }

})();
